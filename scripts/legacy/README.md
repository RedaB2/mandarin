# Legacy Launchers

These wrappers are deprecated compatibility entrypoints that forward to the single cross-platform launcher:

- `python run.py`

They are kept here to avoid cluttering the repository root.
